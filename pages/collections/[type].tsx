import React from 'react';
import { useRouter } from 'next/router';

const Type = () => {
  const router = useRouter();
  return (
    <div>
      <h1>collections</h1>
    </div>
  );
};

export default Type;
