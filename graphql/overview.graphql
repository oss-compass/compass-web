query getOverview {
    overview {
        issuesCount
        pullsCount
        reposCount
        stargazersCount
        subscribersCount
        trends {
            backend
            forksCount
            issuesCount
            language
            name
            openIssuesCount
            path
            pullsCount
            stargazersCount
            watchersCount
        }
    }
}
